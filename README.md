# AI Requirement Engineering Assistant

A Streamlit app for automated requirements extraction, user story generation, test case generation, quality evaluation, traceability, and documentation.

## Setup

1. Create a Python virtual environment.
2. Install dependencies: `pip install -r requirements.txt`
3. Run: `python app.py`
