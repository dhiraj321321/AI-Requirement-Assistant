# Frontend (React) for AI Requirement Engineering Assistant

This folder contains a Vite + React frontend skeleton that consumes the existing Python backend.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend API at `http://localhost:8000` (configured in `frontend/src/pages/Login.jsx`).
